"""
Utilities Module.

Contains utilities such as:
* ``polyvalent_functions.py`` -  Functions used in different endpoints;
* ``validate.py`` -  Validation of JSON schemas and request parameters;
* ``models.py`` -  Dictionaries and functions of the different models;
* ``access_levels.yml`` -  Information about the access levels of this beacon;
* ``translate2accesslevels.py`` -  Dictionaries that link the objects of the responses of the main endpoints to
the name they have in the access levels file, so they can be compared in the filtering step;
"""
